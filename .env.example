BOT_TOKEN=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
ADMIN_TELEGRAM_ID=
WEBHOOK_SECRET=
WEBHOOK_URL=https://bot.corneriq.site/api/webhook
PROD_WEBHOOK_URL=https://bot.corneriq.site/api/webhook

# Wayl belongs to the bot deployment. Keep test until a separate live release.
WAYL_API_KEY=
WAYL_WEBHOOK_SECRET=
WAYL_ENV=test
WAYL_SITE_URL=https://bot.corneriq.site
CRON_SECRET=

# NOTE: Your Supabase database schema needs these columns for notifications:
# - users.telegram_id (to notify students about order status)
# - staff.telegram_id (to notify cashiers about new orders)
# Both should store the raw Telegram user ID (numeric string).
